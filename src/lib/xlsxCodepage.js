// يسجّل جداول الترميزات القديمة (codepages) لمكتبة xlsx — بلا هذا التسجيل، ملفات
// .xls القديمة (BIFF) المُشفَّرة بترميزات غير UTF-16/UTF-8 — أشهرها Windows-1256
// العربي — تُقرَأ بحروف مشوَّهة (mojibake) في المتصفح فقط.
//
// السبب الجوهري: بنية xlsx الخاصة بـNode (xlsx.js، المُستخدَمة تلقائياً بالاختبارات
// عبر vitest) تحمّل جدول الترميزات تلقائياً بنفسها (require('./dist/cpexcel.js')
// داخل الحزمة نفسها). بنية xlsx الخاصة بالمتصفح/Vite (xlsx.mjs) لا تحمّله تلقائياً
// إطلاقاً — فقط تعرض دالة set_cptable() ليستدعيها التطبيق يدوياً. لهذا بالضبط نجحت
// اختبارات chartOrganizerAgent.columnSynonyms.test.js (Node) بينما نفس الملف فعلياً
// فشل بالمتصفح (تحديداً على الرؤوس العربية الصريحة كـ"الرمز"/"الاسم" المكتوبة داخل
// .xls بترميز Windows-1256 — رموز acc_arab/acc_lati/acc_levl لاتينية ASCII خالصة
// فما تأثرت بهذا الخطأ، فما ظهر إلا هنا).
//
// استيراد هذا الملف مرة واحدة فقط، من main.jsx (أقرب نقطة ممكنة لبداية التطبيق)،
// كافٍ لتصحيح كل قراءات XLSX.read بكل أدوات التطبيق (المرجع الداخلي لجدول
// الترميزات مُشترَك بين كل استيرادات "xlsx" داخل نفس التطبيق) — بلا أي تعديل على
// أي من الملفات التي تقرأ ملفات إكسل فعلياً.
import * as XLSX from "xlsx";
import * as cptable from "xlsx/dist/cpexcel.full.mjs";

XLSX.set_cptable(cptable);
