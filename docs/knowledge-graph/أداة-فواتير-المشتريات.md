# مطابقة واستيراد فواتير المشتريات (Bill Import)

**المفتاح:** `tool.bills` · **المجلد:** `src/bill-import/`

تحوّل ملف مشتريات غير منظم إلى قالب قيود المعتمد، وترسل الفواتير مباشرة عبر `POST /bills`.

## البنية
- `lib/fields.js` + `lib/mapping.js` (`autoMap`) — ربط أعمدة تلقائي بالاسم وشكل القيم معًا.
- `lib/clientFile.js` — قراءة الملف وبناء الصفوف مجمَّعة بالفاتورة (`groupsOf`).
- `lib/validation.js` — كل قواعد تحقق قيود (السعر مع وحدة التحويل، الخصم، التواريخ…).
- `lib/billsPush.js` — إرسال مباشر عبر [[طبقة-الشبكة-والـAPI]].
- `lib/template.js` — قراءة/كتابة القالب عبر JSZip (يحافظ على التنسيق والقوائم المنسدلة).

## مرتبط
- [[نظرة-عامة]]
- [[أداة-الموردين]] — الطرف المحاسبي المقابل: كل فاتورة مشتريات تُنسَب لمورد.
- [[بنية-الأدوات-المشتركة]] — القرابة البرمجية مع بقية الأدوات.
- [[التبويبات-المتعددة]]
